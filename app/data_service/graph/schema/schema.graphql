# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Query {
    todos: [Todo!]!
}

type Mutation {
    createTodo(payload: String): Todo
}

schema {
    query: Query
    mutation: Mutation
}